server:
  port: 8083

spring:
  application:
    name: sensor-access-service

alert:
  service-url: ${ALERT_SERVICE_URL:http://localhost:8090}

internal:
  token: ${INTERNAL_TOKEN:stark-internal-token}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics


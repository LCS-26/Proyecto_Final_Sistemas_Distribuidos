server:
  port: 8090

spring:
  application:
    name: alert-service

internal:
  token: ${INTERNAL_TOKEN:stark-internal-token}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

